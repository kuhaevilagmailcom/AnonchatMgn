services:
  anonchat-mgn:
    build: .
    container_name: anonchat-mgn
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    # раскомментируй, если переведёшь бота на вебхук вместо polling
    # ports:
    #   - "8081:8081"
